/**
 * ProblemsPage — a library of classic DSA problems you can dry run in one
 * click.
 *
 * Placeholder for now. In Phase 7 this reads a list of problems from
 * src/data/problems/ — plain JSON files, so adding a problem never means
 * touching the code.
 */

import { ComingSoon } from "../../components/ui/ComingSoon.jsx";

export function ProblemsPage() {
  return (
    <ComingSoon
      phase="Phase 7"
      title="The problem library"
      description="Two pointers, sliding window, linked lists, trees, graphs, DP and backtracking — each with a worked solution you can open and step through immediately."
    />
  );
}
