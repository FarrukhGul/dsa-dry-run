/**
 * WhiteboardPage — a hand-drawn drawing board for sketching out an approach.
 *
 * Placeholder for now. In Phase 5 this page embeds Excalidraw (which is open
 * source, MIT licensed) and adds a shape library of DSA pieces: nodes, array
 * cells, trees and stack frames.
 */

import { ComingSoon } from "../../components/ui/ComingSoon.jsx";

export function WhiteboardPage() {
  return (
    <ComingSoon
      phase="Phase 5"
      title="The whiteboard"
      description="Sketch the idea before you write it. A full hand-drawn canvas, in the same style as the rest of the app, with ready-made shapes for nodes, arrays and trees."
    />
  );
}
